#include "resource.h"
#include "task.h"


int main()
{
    //activate_task(0);
    //activate_task(1);
printf("")
    while (1){
	// scheduler(reay Queue)
	// readyQ->head > running state
	
    }
    return 0;
}
